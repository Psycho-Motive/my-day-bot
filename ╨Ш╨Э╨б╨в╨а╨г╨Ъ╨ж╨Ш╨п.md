# 🤖 МОЙ ДЕНЬ — Инструкция по запуску бота

## ШАГ 1 — Получи токен бота (2 минуты)

1. Открой Telegram, найди @BotFather
2. Напиши ему `/newbot`
3. Придумай имя бота, например: `Мой Дневник`
4. Придумай username (латиницей, заканчивается на bot), например: `my_day_diary_bot`
5. BotFather пришлёт токен вида: `7123456789:AAFxxx...`
6. **Скопируй этот токен** — он нужен на следующем шаге

---

## ШАГ 2 — Вставь токен в код

Открой файл `bot.py`, найди строку:

```python
TOKEN = os.environ.get("BOT_TOKEN", "ВАШ_ТОКЕН_ЗДЕСЬ")
```

Замени `ВАШ_ТОКЕН_ЗДЕСЬ` на свой токен:

```python
TOKEN = os.environ.get("BOT_TOKEN", "7123456789:AAFxxx...")
```

---

## ШАГ 3 — Запусти бота (выбери один способ)

### 🅐 Локально на компьютере (для теста)

```bash
# Установи Python 3.10+ если нет
pip install -r requirements.txt
python bot.py
```

Бот работает пока открыт терминал. Для постоянной работы — используй вариант Б.

---

### 🅑 Бесплатный хостинг Railway.app (рекомендую)

1. Зайди на **railway.app** → войди через GitHub
2. Нажми **"New Project"** → **"Deploy from GitHub repo"**
3. Загрузи три файла: `bot.py`, `requirements.txt`, `Procfile`
4. Зайди в **Variables** → добавь переменную:
   - Ключ: `BOT_TOKEN`
   - Значение: твой токен от BotFather
5. Нажми **Deploy** — бот запустится автоматически ✅

**Бот будет работать 24/7 бесплатно!**

---

### 🅒 Бесплатный хостинг Render.com

1. Зайди на **render.com** → войди через GitHub
2. Нажми **"New"** → **"Web Service"**
3. Подключи репозиторий с файлами бота
4. Настройки:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`
5. В **Environment Variables** добавь `BOT_TOKEN` = твой токен
6. Нажми **Create Web Service** ✅

---

## ШАГ 4 — Поделись ботом

После запуска у тебя будет ссылка вида:
```
https://t.me/my_day_diary_bot
```

Отправь эту ссылку кому угодно — каждый получит **свой личный дневник**.
Данные каждого пользователя хранятся отдельно.

---

## КАК ПОЛЬЗОВАТЬСЯ БОТОМ

| Действие | Как |
|----------|-----|
| Добавить запись | Просто написать боту любой текст |
| Посмотреть сегодня | `/today` |
| Посмотреть вчера | `/yesterday` |
| История 7 дней | `/history` |
| Очистить сегодня | `/clear` |
| Главное меню | `/menu` |
| Оценить день | Нажать ⭐ в сводке |
| Настроение | Нажать эмодзи в сводке |

**Вечерняя сводка** приходит автоматически в 21:00 по московскому времени.

---

## ПРИМЕРЫ ЗАПИСЕЙ В БОТ

```
Проснулся в 7:30, чувствую себя хорошо
Выехал на работу, пробки небольшие
Провёл планёрку, поставили задачи на неделю
Заехал в Пятёрочку: хлеб, молоко, яйца
Вернулся домой в 19:15
Поужинал, посмотрел серию, читаю книгу
Завтра важная встреча — надо подготовиться
```

Каждое сообщение сохраняется с временем отправки 🕐

---

## ВОПРОСЫ?

Если что-то не работает — проверь:
1. Токен скопирован без пробелов
2. Python версии 3.10 или выше
3. Установлены зависимости из requirements.txt
